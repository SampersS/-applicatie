hoe gaan we de verbinding bevijligen/schrijf functies enkel toelaten aan geautenticeerden?

server: stuurt site met public key en random waarde

client: krijgt public key en random waarden, als hij wilt verstuuren moet hij een checksum van de url/getransporteerde data + random waarde + wachtwoord geëncripteert met de public key in de body doorsturen
daarna desencrypteerd de server de data in de body met de private key, check of de checksum klopt => check of de random waarde hetzelfde is => valideer wachtwoord. als alles goed is dan wordt de bewerking uitgevoert anders niet en krijgt de zender een lelijke response

wachtwoord moet worden opgeslagen in de browser met web storage.
steeds er een geautenticeerde actie wordt uitgevoert moet de random waarde (bijgehouden per ip address) met 1 vermeerderen om aanvallen met dezelfde geincrypteerde data te vermijden.

node functie: get key+random

Voor browsers zonder web storage: altijd een wachtwoordveld ergens op de pagina
